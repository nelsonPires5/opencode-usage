# Enable real auth tests per provider (1 = enabled, 0 = disabled)
# By default, all tests use mocks only
REAL_OPENAI_AUTH=1
REAL_GOOGLE_AUTH=1
REAL_ZAI_CODING_PLAN_AUTH=1

# Legacy aliases (still supported)
REAL_CODEX_AUTH=1
REAL_ANTIGRAVITY_AUTH=1
REAL_ZAI_AUTH=1
